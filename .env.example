# OpenAI API Configuration
OPENAI_API_KEY=sk-proj-your-openai-api-key-here
OPENAI_FAST_MODEL=gpt-5
OPENAI_EXTRACTOR_MODEL=gpt-5

# Supabase Storage Configuration
# 프로덕션(Vercel)에서는 반드시 SERVICE_KEY를 사용하세요
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key-here
SUPABASE_ANON_KEY=your-anon-key-here

# 주의사항:
# 1. SUPABASE_SERVICE_KEY는 서버 사이드 전용입니다 (절대 클라이언트에 노출 금지)
# 2. Vercel 환경에서는 SERVICE_KEY가 필수입니다 (RLS 제약 없이 작동)
# 3. ANON_KEY만 사용하면 Supabase RLS 권한 에러가 발생할 수 있습니다
